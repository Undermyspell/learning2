import { ThemeableDirective } from './themeable.directive';

describe('ThemeableDirective', () => {
  it('should create an instance', () => {
    const directive = new ThemeableDirective();
    expect(directive).toBeTruthy();
  });
});
