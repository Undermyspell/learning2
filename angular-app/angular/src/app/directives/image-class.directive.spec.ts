import { ImageClassDirective } from './image-class.directive';

describe('ImageClassDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageClassDirective();
    expect(directive).toBeTruthy();
  });
});
