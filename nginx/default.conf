upstream frontend {
  server mkfrontend.herokuapp.com;
}

upstream backend {
  server mkbackend.herokuapp.com;
}

server {
  listen $PORT;
  
  location / {
    proxy_pass http://frontend;
    proxy_set_header Host mkfrontend.herokuapp.com;
  }

  location /api {
    proxy_pass http://backend;
    proxy_set_header Host mkbackend.herokuapp.com;
  }
}